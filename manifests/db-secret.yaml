# DEMO ONLY: giá trị dưới đây là mật khẩu test cho cluster kind local, KHÔNG dùng lại
# cho môi trường thật. Vì repo này là Public, mọi giá trị ở đây coi như đã công khai.
# Cho production: dùng Sealed Secrets / External Secrets / SOPS thay vì commit Secret thẳng vào Git.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  POSTGRES_USER: todolist
  POSTGRES_PASSWORD: demo-only-not-a-real-secret
  POSTGRES_DB: todolist
